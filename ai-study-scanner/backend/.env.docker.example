# Copy to .env (same folder) and fill in secrets before running docker compose.

# FastAPI app
ENV=prod
LOG_LEVEL=info
PORT=8000

# Gemini (required for /solve)
GEMINI_API_KEY=YOUR_KEY_HERE
GEMINI_MODEL=gemini-1.5-flash
GEMINI_TIMEOUT_S=30.0
